datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Favorite {
  id        Int    @id @default(autoincrement())
  name      String
  type      String  // "skin" | "emote"
  createdAt DateTime @default(now())
}